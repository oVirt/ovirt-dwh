ovirt-engine-dwhd service configuration directory

Only *.conf are considered.

Files are sorted by name, last wins.

Variables available at:

  @PKG_DATA_DIR@/services/ovirt-engine-dwhd/ovirt-engine-dwhd.conf
