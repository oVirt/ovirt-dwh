#!/bin/bash
#


# ETL functions library.
. /usr/share/ovirt-engine-dwh/etl/etl-common-functions.sh

if [ ! -e /usr/share/ovirt-engine-dwh/etl/kill ]; then
    pid=0
    procrunning > /dev/null 2>&1
    if [ $pid = '0' ]; then
        service ovirt-engine-dwhd start > /dev/null 2>&1
    fi
fi
