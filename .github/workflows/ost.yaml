name: ost
on:
  workflow_run:
    workflows: ["build"]
    types:
      - completed
      
jobs:
  on-success:
    runs-on: self-hosted
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.review.state == 'approved' }}
    steps:
      - name: trigger
        run: >
          echo "hi,$(id)" > /tmp
